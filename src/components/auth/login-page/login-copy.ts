/** Hero layout matches the product home; copy is chat-only. */
export const LOGIN_COPY = {
  brand: "AbabilX Chat",
  badge: "End-to-end encrypted",
  headline: "One place for",
  headlineAccent: "personal & workspace chat.",
  signInGoogle: "Sign in with Google",
  signInQr: "Sign in with QR",
} as const;

export const LOGIN_BUTTON_CLASS =
  "inline-flex h-[clamp(2.5rem,4.5vw,3rem)] items-center gap-3 rounded-xl border border-[#dadce0] bg-white px-[clamp(1rem,2vw,1.5rem)] text-[clamp(13px,1.2vw,15px)] font-medium text-[#3c4043] shadow-[0_1px_2px_rgba(60,64,67,0.15),0_1px_3px_rgba(60,64,67,0.1)] transition-all hover:bg-[#f8f9fa] hover:shadow-[0_2px_6px_rgba(60,64,67,0.15)] active:scale-[0.98]";
