"use client";

/**
 * The bubble's right-click menu is the app's floating menu; this is only the
 * name the timeline knows it by.
 */
export { default, type FloatingMenuItem as BubbleMenuItem } from "@/components/shared/floating-menu";
